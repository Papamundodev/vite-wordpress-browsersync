{
  "name": "vite",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "browser-sync start --proxy 'vite.local' --files 'resources/**/*.css, resources/**/*.js, **/*.php' --no-open",
    "build": "vite build",
    "preview": "vite preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
